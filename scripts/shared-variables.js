'use strict';

console.log('--- loading: global variables');

const instructions = `To start please type in 2 number into input area and choose the operator (+ , - , * , /).
In order to continue calculate with your Last Result please enter only one input (either first or second). 
For example: 
first operation: 2 * 2 = 4
second operation: 2 *  = 2 * result of first operation (4) = 8. `;

let lastResult = 0;
